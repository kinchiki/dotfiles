---
name: ticket-to-plan
description: >-
  GitHub や Linear のチケット、またはユーザーの自然言語の変更依頼を調査し、承認済みの実装プランとユーザー向け情報ファイルに変換する。
  ユーザーがチケットを示したとき、変更依頼を計画へ変換したいとき、または実装前に設計、範囲、タスク分解を確定したいときに使う。
  コンテキストに余裕があれば同一セッションで `implement-plan` による実装へ進み、余裕が乏しい場合だけ別セッションへ引き継ぐ。
---

# ticket-to-plan

チケットまたは自然言語の変更依頼から、新しいセッションがそのまま実装できる永続的な実装プランとユーザー向け情報ファイルを作る。
計画中は read-only を保ち、承認後に保存した実装プランを実装の契約とする。

## 対象範囲

- 入力種別を判定し、目的、対象、制約、受入基準を確定する。
- コードベースと既存 pattern を調査し、実装方針を選択する。
- 選択した方針を検証可能なタスクへ分解する。
- ユーザー確認と独立AIレビューを経て実装プランを確定する。
- 実装プランとユーザー向け情報ファイルを現在の AI agent に対応する plan directory の `<plan-id>/` 配下に保存する。
- 保存後、同一セッションでの実装または別セッションへの引き継ぎを選ぶ。

## 参照先

- `references/source-resolution.md`: Step 1 で入力種別を判定して内容を取得する前に読む。
- `../ask-user-questions/SKILL.md`: Step 1 または Step 2 で、調査では解けない質問や設計方針の選択をユーザーへ求める直前に読む。
- `references/approach-selection.md`: Step 2 の codebase 調査と方針選択を始める前に読み、Step 3 のタスク分解まで従う。
- `../ai-review/references/test-selection-policy.md`: Step 3 でテスト方針と各タスクの `test` を決める直前に読む。
- `references/approval-flow.md`: Step 4 で草案を提示する前に読み、Step 6 の最終承認まで従う。
- `../ai-review/SKILL.md`: Step 5 でユーザーレビュー済みの草案を独立レビューへ渡す直前に読む。
- `references/plan-template.md`: Step 7 で実装プランとユーザー向け情報ファイルを保存する直前に読む。
- `../implement-plan/SKILL.md`: Step 8 で同一セッションの実装開始が承認された後、実装へ進む直前に読む。

## 必須制約

- orchestrator は現在の AI agent で利用できる上位推論モデルを使う。満たせない、または確認できない場合は一度警告し、ユーザーが明示的に品質上の不利益を受け入れた場合だけ続行する。
- Step 2 の codebase 調査は `codebase-investigator` へ委譲する。
- orchestrator はコードベースを直接調査しない。直接読んでよいのは worker の返却、plan file、repo convention file、および `## 参照先` が指すスキルと reference file とする。コードベースの追加の事実が必要な場合は worker へ再委譲するか、ユーザーへ確認する。
- 計画中は production code を編集しない。
- repo 調査で解けない、実装可否または受入基準を左右する不明点は推測で埋めず、ユーザーへ確認する。
- non-simple task では、ユーザーが実装方針を選択するまで詳細なタスク分解を作らない。
- 独立AIレビューの全指摘はユーザーが項目ごとに採用または見送りを判断し、計画AIが単独で採否や方針変更を確定しない。
- 実装プランは、入力やユーザー向け情報ファイルを再取得しなくても実装を開始できる内容にする。
- 発生可能性と影響に見合う事象だけを専用実装として計画し、低確率で単純なエラー処理で足りる事象は単純な処理で対処する。
- 最終承認は計画内容の承認として扱い、実装開始は Step 8 で別途確認する。
- 最終承認の履歴は保存ファイルに記録しない。

## オーケストレーション

- **Step 0: モデルを確認する。** orchestrator が上位推論モデルであることと、調査を委譲する worker が Claude=`sonnet` / Codex=`gpt-5.6-luna`, effort=`medium` であることを確認し、使用モデルと品質上の不利益を実装プランのヘッダーに残す。
- **Step 1: 入力を解決する。** 入力の全内容を取得または抽出し、3〜6行の入力要約をユーザーへ返して、計画を左右する不足を確認する。
- **Step 2: 調査して方針を選ぶ。** コードベースの read-only 調査を `codebase-investigator` へ委譲し、その返却をもとに simple / non-simple を判定して、必要な代替案比較とユーザー選択を完了する。
- **Step 3: タスクへ分解する。** 選択済みの方針を、対象ファイル、依存関係、テスト、観測可能な完了条件を持つタスクへ分解する。
- **Step 4: 草案レビューを受ける。** 実装プラン要約、タスク分解、ユーザー向け情報の草案を提示し、ユーザーの修正を反映する。
- **Step 5: 独立AIレビューを実行する。** ユーザーレビュー済みの最新草案を別系統の AI へ渡し、信頼性判定と指摘を受け取る。
- **Step 6: 指摘の採否と最終承認を得る。** 全指摘の判断をユーザーへ求め、必要な再調査と再選択を行った最終版を再提示する。
- **Step 7: 計画を保存する。** テンプレートとパス規約に従って2ファイルを保存し、ユーザー向け情報ファイルの全内容を表示する。
- **Step 8: 実装を続行または引き継ぐ。** デフォルトは同一セッションとし、実装開始の承認を得てから `implement-plan` を起動する。コンテキスト不足または大規模・high risk の場合は、working directory、実装プランの絶対 path、ユーザー依頼なら入力要約と plan ID を含む自己完結した引き継ぎ文を1つの fenced code block で提示し、実装や別 agent の起動を行わず停止する。選んだ経路、理由、入力要約、保存先、主要方針、タスク概要、AIレビュー結果を報告する。
